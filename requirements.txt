# --- Retrieval pipeline (rag.py) ---
qdrant-client>=1.9.0
sentence-transformers>=3.0.0
fastembed>=0.3.0
 
# --- LLM integration layer (llm.py) ---
requests>=2.31.0
python-dotenv>=1.0.0
 
# --- API layer (api_example.py) — remove if not deploying the API yet ---
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
 